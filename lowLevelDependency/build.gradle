apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])

    compileOnly "com.google.dagger:dagger-compiler:2.4"
    compile "com.google.dagger:dagger:2.4"
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
